<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Putter | Betting Infrastructure</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Data driven betting tools to track, analyze, and allocate. Portfolio management for sports bettors." />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
 
<link rel="icon" type="image/png" href="putter.png?v=2" />
<link rel="shortcut icon" type="image/png" href="putter.png?v=2" />
  
  <style>
    :root {
      --bg: #050509;
      --bg-grad: radial-gradient(circle at 50% 0%, #13131f 0%, #050509 60%);
      --surface: #0c0c12;
      --surface-highlight: #16161e;
      --border: rgba(255, 255, 255, 0.08);
      --accent: #39ff88;
      --accent-glow: rgba(57, 255, 136, 0.3);
      --text: #ededf2;
      --muted: #888b99;
      --font-main: "Inter", sans-serif;
      --font-mono: "JetBrains Mono", monospace;
    }
    
    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: var(--font-main);
      background: var(--bg);
      background-image: var(--bg-grad);
      color: var(--text);
      min-height: 100vh;
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }

    a { color: inherit; text-decoration: none; transition: 0.2s; }
    
    /* Utility Animations */
    @keyframes pulse-green {
      0% { box-shadow: 0 0 0 0 rgba(57, 255, 136, 0.4); }
      70% { box-shadow: 0 0 0 6px rgba(57, 255, 136, 0); }
      100% { box-shadow: 0 0 0 0 rgba(57, 255, 136, 0); }
    }

    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-5px); }
      100% { transform: translateY(0px); }
    }

    .page { max-width: 1080px; margin: 0 auto; padding: 0 20px 80px; }

    /* NAV */
    .nav {
      display: flex; align-items: center; justify-content: space-between;
      padding: 24px 0; margin-bottom: 40px;
    }

    .brand { display: flex; align-items: center; gap: 12px; }
    .brand-mark {
      width: 32px; height: 32px;
      background: linear-gradient(135deg, #fff, var(--accent));
      border-radius: 6px;
      display: grid; place-items: center;
      color: #000; font-weight: 800; font-size: 18px;
    }
    .brand-text { font-weight: 700; letter-spacing: -0.02em; font-size: 16px; }
    .beta-tag {
      font-size: 10px; padding: 2px 8px; border-radius: 99px;
      background: rgba(57, 255, 136, 0.1); color: var(--accent);
      border: 1px solid rgba(57, 255, 136, 0.2);
      text-transform: uppercase; letter-spacing: 0.05em;
      font-weight: 600;
    }

    /* HERO */
    .hero {
      display: grid; grid-template-columns: 1.2fr 1fr; gap: 64px;
      align-items: center; margin-bottom: 80px;
    }

    .hero-content h1 {
      font-size: clamp(2.5rem, 5vw, 3.5rem);
      line-height: 1.1; font-weight: 700; letter-spacing: -0.03em;
      margin-bottom: 20px;
    }
    
    .text-gradient {
      background: linear-gradient(to right, #fff, #94a3b8);
      -webkit-background-clip: text; background-clip: text; color: transparent;
    }
    
    .hero-content p {
      color: var(--muted); font-size: 1.1rem; margin-bottom: 32px; max-width: 440px;
    }

    .cta-group { display: flex; gap: 12px; align-items: center; }
    
    .btn-primary {
      background: var(--text); color: var(--bg);
      padding: 12px 24px; border-radius: 8px;
      font-weight: 600; font-size: 14px;
      border: 1px solid transparent;
    }
    .btn-primary:hover { background: #fff; transform: translateY(-1px); }

    .btn-ghost {
      color: var(--muted); padding: 12px 24px;
      font-size: 14px; font-weight: 500;
    }
    .btn-ghost:hover { color: var(--text); }

    /* HERO CARD (The Terminal) */
    .terminal-card {
      background: rgba(12, 12, 18, 0.6);
      backdrop-filter: blur(20px);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 24px;
      box-shadow: 0 40px 80px -20px rgba(0,0,0,0.8);
      position: relative;
      animation: float 6s ease-in-out infinite;
    }
    
    .terminal-card::before {
      content:''; position: absolute; top: -1px; left: -1px; right: -1px; height: 1px;
      background: linear-gradient(90deg, transparent, var(--accent), transparent);
      opacity: 0.5;
    }

    .stat-row {
      display: flex; align-items: center; justify-content: space-between;
      padding: 12px; border-radius: 8px;
      background: rgba(255,255,255,0.03);
      margin-bottom: 8px; border: 1px solid transparent;
      transition: 0.2s;
    }
    .stat-row:hover { border-color: rgba(255,255,255,0.1); background: rgba(255,255,255,0.05); }

    .stat-info h4 { font-size: 13px; font-weight: 500; color: var(--text); }
    .stat-info span { font-size: 11px; color: var(--muted); display: block; }
    
    .status-dot {
      width: 6px; height: 6px; border-radius: 50%; background: var(--accent);
      box-shadow: 0 0 10px var(--accent);
      animation: pulse-green 2s infinite;
    }

    .tag {
      font-size: 10px; font-family: var(--font-mono);
      padding: 4px 8px; border-radius: 4px;
      text-transform: uppercase; letter-spacing: 0.05em;
    }
    .tag.live { color: var(--accent); background: rgba(57, 255, 136, 0.1); }
    .tag.beta { color: #facc15; background: rgba(250, 204, 21, 0.1); }

    /* FEATURES GRID */
    .features {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px;
      margin-top: 40px;
    }
    .feature-box {
      padding: 24px; border-radius: 12px;
      border: 1px solid var(--border);
      background: var(--surface);
      transition: 0.2s;
    }
    .feature-box:hover { border-color: rgba(255,255,255,0.2); }
    .feature-icon { font-size: 20px; margin-bottom: 16px; display: block; opacity: 0.8; }
    .feature-box h3 { font-size: 15px; font-weight: 600; margin-bottom: 8px; }
    .feature-box p { font-size: 13px; color: var(--muted); line-height: 1.6; }

    /* ROADMAP TIMELINE */
    .roadmap-section { margin-top: 100px; }
    .section-header { margin-bottom: 32px; }
    .section-header h2 { font-size: 20px; font-weight: 600; margin-bottom: 8px; }
    .section-header p { color: var(--muted); font-size: 14px; }

    .timeline { position: relative; padding-left: 24px; border-left: 1px solid var(--border); }
    
    .timeline-item { position: relative; margin-bottom: 32px; padding-left: 24px; }
    .timeline-item::before {
      content: ''; position: absolute; left: -29px; top: 6px;
      width: 9px; height: 9px; border-radius: 50%;
      background: var(--surface); border: 2px solid var(--muted);
      transition: 0.3s;
    }
    .timeline-item.active::before {
      border-color: var(--accent); background: var(--accent);
      box-shadow: 0 0 15px var(--accent-glow);
    }

    .t-meta {
      font-family: var(--font-mono); font-size: 11px; color: var(--muted);
      text-transform: uppercase; letter-spacing: 1px; margin-bottom: 4px;
    }
    .t-title { font-size: 15px; font-weight: 500; color: var(--text); margin-bottom: 4px; }
    .t-desc { font-size: 13px; color: #64748b; max-width: 500px; }

    /* WAITLIST CTA */
    .waitlist-box {
      margin-top: 80px;
      background: linear-gradient(180deg, rgba(57, 255, 136, 0.05), transparent);
      border: 1px solid var(--border);
      border-radius: 16px; padding: 40px;
      text-align: center;
    }
    
    .email-input-container {
      display: inline-flex; align-items: center;
      background: #000; border: 1px solid var(--border);
      border-radius: 8px; padding: 6px;
      margin-top: 24px; width: 100%; max-width: 360px;
    }
    
    .fake-input {
      background: transparent; border: none; color: #fff;
      font-size: 13px; flex: 1; padding: 0 12px; outline: none;
    }
    
    .copy-btn {
      background: var(--surface-highlight); color: #fff;
      border: 1px solid var(--border); border-radius: 6px;
      padding: 8px 16px; font-size: 12px; font-weight: 500;
      cursor: pointer; transition: 0.2s;
    }
    .copy-btn:hover { background: var(--accent); color: #000; border-color: var(--accent); }
    
    .copy-feedback {
      margin-top: 12px; font-size: 12px; color: var(--accent);
      height: 16px; opacity: 0; transition: opacity 0.3s;
    }

    /* RESPONSIVE */
    @media (max-width: 800px) {
      .hero { grid-template-columns: 1fr; gap: 40px; }
      .features { grid-template-columns: 1fr; }
      .terminal-card { order: -1; }
    }
  </style>
</head>
<body>

  <div class="page">
    <nav class="nav">
      <div class="brand">
        <div class="brand-mark">P</div>
        <div class="brand-text">PUTTER</div>
        <div class="beta-tag">Private Beta</div>
      </div>
      <a href="#access" class="btn-ghost" style="font-size:13px;">Log In</a>
    </nav>

    <main class="hero">
      <div class="hero-content">
        <h1>Betting infrastructure <br /> for <span class="text-gradient">sharper decisions.</span></h1>
        <p>
          Putter is a suite of tools for people who treat betting like a portfolio, not a parlay. Track performance, surface edges, and allocate with intent.
        </p>
        <div class="cta-group">
          <a href="#roadmap" class="btn-primary">Explore Roadmap</a>
          <a href="#access" class="btn-ghost">Request Access</a>
        </div>
      </div>

      <div class="terminal-card">
        <div style="display:flex; justify-content:space-between; margin-bottom:16px; font-size:11px; color:var(--muted);">
          <span>SYSTEM STATUS</span>
          <div style="display:flex; align-items:center; gap:6px;">
             <div class="status-dot"></div>
             <span style="color:var(--accent)">OPERATIONAL</span>
          </div>
        </div>

        <div class="stat-row">
          <div class="stat-info">
            <h4>PVI Analyzer</h4>
            <span>Quantify edge vs. closing line</span>
          </div>
          <div class="tag live">v1.0 Live</div>
        </div>

        <div class="stat-row">
          <div class="stat-info">
            <h4>Allocation Engine</h4>
            <span>Kelly Criterion & Fixed Unit sizing</span>
          </div>
          <div class="tag beta">In Build</div>
        </div>

        <div class="stat-row" style="opacity:0.5">
          <div class="stat-info">
            <h4>Book Scanner</h4>
            <span>Cross-market arbitrage detection</span>
          </div>
          <div class="tag">Q3 2025</div>
        </div>

        <div style="margin-top:20px; padding-top:16px; border-top:1px solid rgba(255,255,255,0.05); display:flex; justify-content:space-between;">
          <div class="stat-info">
            <span>PVI (YTD)</span>
            <h4 style="color:var(--accent); font-family:var(--font-mono);">1.62x</h4>
          </div>
          <div class="stat-info" style="text-align:right;">
            <span>TICKETS</span>
            <h4 style="font-family:var(--font-mono);">542</h4>
          </div>
        </div>
      </div>
    </main>

    <section class="features">
      <div class="feature-box">
        <span class="feature-icon">📊</span>
        <h3>Track like an analyst</h3>
        <p>Every ticket has a home. View performance by player, market, and season, not just vibes after a win or loss.</p>
      </div>
      <div class="feature-box">
        <span class="feature-icon">🎯</span>
        <h3>Quantify your edge</h3>
        <p>Tools like <strong>PVI</strong> turn "nice hit" into measurable over-performance relative to the odds you accepted.</p>
      </div>
      <div class="feature-box">
        <span class="feature-icon">⚖️</span>
        <h3>Allocate with intent</h3>
        <p>One unit shouldn't always mean one bet. Use our calculator to spread risk across a card without guessing.</p>
      </div>
    </section>

    <section class="roadmap-section" id="roadmap">
      <div class="section-header">
        <h2>Build Roadmap</h2>
        <p>We are rolling out features in waves to ensure data accuracy.</p>
      </div>

      <div class="timeline">
        <div class="timeline-item active">
          <div class="t-meta">Phase 1 · Current</div>
          <div class="t-title">Foundation & Tracking</div>
          <div class="t-desc">Manual ticket entry, Golf PVI analyzer, and basic dashboarding.</div>
        </div>
        <div class="timeline-item">
          <div class="t-meta">Phase 2 · Next</div>
          <div class="t-title">Allocation & Integrations</div>
          <div class="t-desc">Google Sheets import sync, bet splitting calculator, and line comparison helpers.</div>
        </div>
        <div class="timeline-item">
          <div class="t-meta">Phase 3 · Future</div>
          <div class="t-title">Verified Records</div>
          <div class="t-desc">Public profiles for handicappers who want to prove their record is real.</div>
        </div>
      </div>
    </section>

    <section class="waitlist-box" id="access">
      <h2>Join the quiet few.</h2>
      <p style="color:var(--muted); font-size:14px; margin-top:8px;">We accept new users in small batches to maintain server stability.</p>
      
      <div style="display:flex; flex-direction:column; align-items:center;">
        <div class="email-input-container">
          <input type="text" class="fake-input" value="hi@putter.bet" readonly id="email-field">
          <button class="copy-btn" id="copy-btn">Copy Email</button>
        </div>
        <div class="copy-feedback" id="feedback">Address copied. Send us a note.</div>
      </div>
    </section>

    <footer style="margin-top:80px; padding-top:24px; border-top:1px solid var(--border); font-size:12px; color:var(--muted); display:flex; justify-content:space-between;">
      <span>© 2025 Putter. Built for the long game.</span>
      <span>No touts. No locks.</span>
    </footer>
  </div>

  <script>
    const copyBtn = document.getElementById('copy-btn');
    const emailField = document.getElementById('email-field');
    const feedback = document.getElementById('feedback');

    copyBtn.addEventListener('click', () => {
      emailField.select();
      navigator.clipboard.writeText(emailField.value);
      copyBtn.innerText = "Copied!";
      copyBtn.style.background = "var(--accent)";
      copyBtn.style.color = "#000";
      feedback.style.opacity = "1";
      
      setTimeout(() => {
        copyBtn.innerText = "Copy Email";
        copyBtn.style.background = "";
        copyBtn.style.color = "";
        feedback.style.opacity = "0";
      }, 2500);
    });
  </script>
</body>

</html>
